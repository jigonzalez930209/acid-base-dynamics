# pnpm 10+: do not fail install when optional dep build scripts (e.g. msw via vitest) are skipped.
# Approved builds are listed in pnpm-workspace.yaml (allowBuilds) and package.json (pnpm.onlyBuiltDependencies).
strict-dep-builds=false
